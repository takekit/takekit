# 09-jev (metadata only)

Ported from ai-content-agent for the takekit prototype.

**Included:** briefing, edit JSON (plan, blocks, jobs, cuts, transcripts), motion-v2 tools/source (no renders), storyboard HTML, caption style config, cover PNGs.

**Omitted:** raw `.mov`, `exports/*.mp4`, `edit/bake/`, `edit/overlay/` stills, audio wav, motion renders, node_modules.

Point Resolve at media on the original ai-content-agent checkout or re-ingest when running a real edit.
Default style lock: `../../resolve/DEFAULT.md`.
