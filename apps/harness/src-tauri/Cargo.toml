[package]
name = "takekit"
version = "0.1.0"
description = "Takekit desktop shell (Tauri 2)"
authors = ["Takekit"]
edition = "2021"

[lib]
name = "takekit_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
tauri-plugin-fs = "2"
tauri-plugin-shell = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri-plugin-dialog = "2"
